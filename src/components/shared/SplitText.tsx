'use client'

import { useEffect } from 'react'
import SplitType from 'split-type'

interface Props {
  text: string
  ref: React.MutableRefObject<any>
}

export default function SplitText({ text, ref }: Props) {}
